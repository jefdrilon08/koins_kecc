%table.table.table-bordered.table-sm.table-hover
  %thead
    %tr
      %th
      %th
        Branch
      %th.text-center
        Date Created
      %th.text-center
        prepared_by
      %th.text-center
        status
  %thead
    - @records.each do |o|
      %tr
        %td.text-center
          = link_to "/billing_for_involuntary/#{o.id}" do
            .bi.bi-search
        %td.text-center
          #{o.meta.with_indifferent_access[:branch_name]}
        %td.text-center
          #{o.meta.with_indifferent_access[:transaction_date]}
        %td.text-center
          #{o.meta.with_indifferent_access["prepared_by"]["name"]}
        
        %td.text-center
          - if o[:status] == "processing"
            .badge.bg-info
              Processing
          - elsif o[:status] == "done"
            .badge.bg-success
              Done
          - elsif o[:status] == "pending"
            .badge.bg-danger
              pending
          - elsif o[:status] == "approved"
            .badge.bg-success
              approved
          - elsif o[:status] == "error"
            .badge.bg-danger
              Error