= render partial: "shared/c_subheader", locals: { items: @subheader_items, side_actions: @subheader_side_actions }

%main.c-main
  .container-fluid
    .row
      .col
        .card
          .card-body
            #loan-application-content

#parameters{ data: { id: "#{params[:id]}", member_id: "#{params[:member_id]}", banks: @banks, controller: "#{params[:controller]}", action: "#{params[:action]}" } }
