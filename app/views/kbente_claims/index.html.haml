%ol.breadcrumb
  %li.breadcrumb-item
    .fa.fa-money-check
    Microinsurance
  %li.breadcrumb-item
    .fa.fa-money-check
    K-Bente Register
  %li.breadcrumb-menu.d-md-down-none
    .btn-group
      = link_to "#", class: 'btn', id: 'btn-new-transaction' do
        .fa.fa-plus
        New Claims
.container-fluid
  .row
    .col
      .card
        .card-body
          - if @kbente_claims.any?
            = render partial: "kbente_claims/list", locals: { kbente_claims: @kbente_claims }
            = paginate @kbente_claims
          - else
            No claims found.

= render partial: "kbente_claims/new_transaction"

= javascript_include_tag "kbente_claims/Index"


:javascript
  $(document).ready(function() {
    var authenticityToken = $("meta[name='csrf-token']").attr('content');

    Index.init({
      authenticityToken: authenticityToken
    });
  });
