= simple_form_for [@claim, @claim_attachment_file] do |f|
  .form-group
    = f.input :file_name, input_html: { class: 'form-control' }, as: :select, collection: ["BLIPFORM", "BC", "MC", "ID", "COHABITATION", "OTHERFILE"]
  .form-group
    = f.input :file, as: :file, input_html: { class: 'form-control' }
  %hr
  .btn-group
    %button.btn.btn-primary{ type: "submit" }
      .fa.fa-check
      Save
    = link_to claim_path(@claim), class: 'btn btn-danger' do
      .fa.fa-times
      Cancel
