= title @record.id
= render partial: "shared/c_subheader", locals: { items: @subheader_items, side_actions: @subheader_side_actions }

%main.c-main
  .container-fluid
    .row
      .col
        .card
          .card-body
            - if @record.error?
              = render partial: "data_stores/error_page"
            - else
              = render partial: "accounting/year_end_closings/data"

= render partial: "accounting/year_end_closings/modals/approve"
= render partial: "templates/error_list"
