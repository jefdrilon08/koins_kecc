= title @accounting_code.code, @accounting_code.name
/ = render partial: "shared/c_subheader", locals: { items: @subheader_items, side_actions: @subheader_side_actions }

%main.c-main
  .container-fluid
    .row
      .col
        .card
          .card-body
            %h5
              #{@accounting_code}
            %hr
