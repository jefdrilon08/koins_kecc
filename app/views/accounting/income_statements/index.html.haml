= title
.row
  .col
    .card
      .card-body
        - if @income_statements.any?
          = render partial: "index_list"
          = paginate @income_statements
        - else
          %p
            No income statements found.

= render partial: "accounting/income_statements/modals/new"
= render partial: "templates/error_list"
