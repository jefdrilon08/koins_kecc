%table.table.table-sm.table-bordered
  %thead
    %th{ width: "5%" }
    %th
      Month
    %th
      Year
    %th
      Branch
    %th
      Created At
    %th.text-center
      Status
  %tbody
    - @income_statements.each do |o|
      %tr
        %td.text-center
          = link_to accounting_income_statement_path(o.id) do
            .fa.fa-search
        %td
          #{Date::MONTHNAMES[o.meta["month"].to_i]}
        %td
          #{o.meta["year"]}
        %td
          #{o.meta["branch_name"]}
        %td
          #{o.created_at.strftime("%B %d, %Y %r")}
        %td.text-center
          - if o.processing?
            .badge.badge-info
              Processing
          - elsif o.done?
            .badge.bg-success
              Done
          - elsif o.error?
            .badge.bg-danger
              Error
