= render partial: "shared/c_subheader", locals: { items: @subheader_items, side_actions: @subheader_side_actions }

%main.c-main
  .container-fluid
    .row
      .col
        .card
          .card-body
            #content

#parameters{ data: { id: params[:id], book: params[:book], accounting_fund_id: params[:accounting_fund_id], branch_id: Settings.try(:defaults).try(:default_branch).try(:id), branch_name: Settings.try(:defaults).try(:default_branch).try(:name) } }

= javascript_include_tag "AccountingEntryForm.min.react"
