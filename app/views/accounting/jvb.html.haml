= render partial: "books_header", locals: { book: "Journal Vouchers" }

.container-fluid
  .row
    .col
      .card
        .card-body
          = render partial: "book_records", locals: { records: @records }
          = paginate @records
