= render partial: "books_header", locals: { book: "JVB", book_title: "General Journal" }

.container-fluid
  .row
    .col
      .card
        .card-body
          = render partial: "filter"
          = render partial: "book_records", locals: { records: @records }
          = paginate @records
