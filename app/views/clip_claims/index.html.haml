%ol.breadcrumb
  %li.breadcrumb-item
    .fa.fa-money-check
    Microinsurance
  %li.breadcrumb-item
    .fa.fa-money-check
    Clip Claims Register
  %li.breadcrumb-menu.d-md-down-none
    .btn-group
      = link_to "#", class: 'btn', id: 'btn-new-transaction' do
        .fa.fa-plus
        New Clip Claims
.container-fluid
  .row
    .col
      .card
        .card-body
          - if @clip_claims.size > 0
            = render partial: "clip_claims/list", locals: { clip_claims: @clip_claims }
            = paginate @clip_claims
          - else
            No clip claims found.

= render partial: "clip_claims/new_transaction"

= javascript_include_tag "clip_claims/Index"


:javascript
  $(document).ready(function() {
    var authenticityToken = $("meta[name='csrf-token']").attr('content');

    Index.init({
      authenticityToken: authenticityToken
    });
  });
