- if ["REMOTE-OAS", "REMOTE-BK", "REMOTE-FM"].exclude? current_user.roles.last
  %li.nav-title
    Adjustments
  %li.nav-item
    = link_to adjustments_subsidiary_adjustments_path, class: "nav-link #{params[:controller] == 'adjustments/subsidiary_adjustments' ? 'active': ''}" do
      %i.nav-icon.fa.fa-file
      Subsidiary
  %li.nav-item
    = link_to adjustments_batch_moratorium_adjustments_path, class: "nav-link #{params[:controller] == 'adjustments/batch_moratorium_adjustments' ? 'active': ''}" do
      %i.nav-icon.fa.fa-file
      Batch Moratorium
