= title @record.id
.h5
  Personal Funds #{@record.data["branch"]["name"]} - #{DateTime.parse(@record.data["as_of"]).strftime("%b %d, %Y")}
  %br
    %br
      .row
        .col
          .card
            .card-body
              - if @record.error?
                = render partial: "data_stores/error_page"
              - else
                #react-root

= render partial: "data_stores/personal_funds/modals/update"
= render partial: "templates/error_list"
