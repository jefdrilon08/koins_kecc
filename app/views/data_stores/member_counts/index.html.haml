= title
= render partial: "index_header"

.container-fluid
  .row
    .col
      .card
        .card-body
          - if @records.any?
            = render partial: "index_list"
            = paginate @records
          - else
            %p
              No records

= render partial: "data_stores/member_counts/modals/new"
= render partial: "templates/error_list"

= javascript_include_tag "data_stores/member_counts/Index"

:javascript
  $(document).ready(function() {
    var authenticityToken = $("meta[name='csrf-token']").attr('content');

    Index.init({
      authenticityToken: authenticityToken
    });
  });
