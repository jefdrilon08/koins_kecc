%table.table.table-bordered.table-sm.table-hover
  %thead
    %tr
      %th
      %th
        Date
      %th
        Branch
      %th.text-center
        Status
      %th.text-center
        Started
      %th.text-center
        Last Updated
  %thead
    - @records.each do |o|
      %tr
        %td.text-center
          = link_to "/data_stores/monthly_incentives/#{o.id}" do
            .bi.bi-search
        %td
          #{o.as_of.to_date.strftime("%B %Y")}
        %td
          #{o.meta["branch_name"]}
        %td.text-center
          - if o.processing?
            .badge.badge-info
              Processing
          - elsif o.done?
            .badge.bg-success
              Done
          - elsif o.error?
            .badge.bg-danger
              Error
        %td.text-center
          %small
            #{o.created_at.localtime}
        %td.text-center
          %small
            #{o.updated_at.localtime}
