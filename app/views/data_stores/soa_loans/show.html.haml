= title @record.id
.h5
  - soaloans = "Soa Loans #{@record.data["branch"]["name"]} - #{DateTime.parse(@record.data["start_date"]).strftime("%b %d, %Y")} to #{DateTime.parse(@record.data["end_date"]).strftime("%b %d, %Y")}"
  = soaloans
  %br
    %br
      .row
        .col
          .card
            .card-body
              - if @record.error?
                = render partial: "data_stores/error_page"
              - else
                #react-root
              #hidden-title{style: "display:none;"}= soaloans
