= render partial: "show_header"

.container-fluid
  .row
    .col
      .card
        .card-body
          - if @record.error?
            = render partial: "data_stores/error_page"
          - else
            = render partial: "show_details"

#parameters{ data: { id: @record.id } }

= javascript_include_tag "BranchResignationShow.min.react"
