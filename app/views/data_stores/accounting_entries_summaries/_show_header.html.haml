%ol.breadcrumb
  %li.breadcrumb-item
    .fa.fa-database
    Data Stores
  %li.breadcrumb-item
    = link_to "/data_stores/accounting_entries_summaries" do
      .fa.fa-database
      Accounting Entries Summaries
  %li.breadcrumb-item
    .fa.fa-file
    #{@record.data.with_indifferent_access[:branch][:name]}
  %li.breadcrumb-menu.d-md-down-none
    .btn-group
      = link_to "#", class: 'btn', id: 'btn-update' do
        .fa.fa-sync
        Update
      = link_to "/data_stores/accounting_entries_summaries/#{@record.id}", method: :delete, class: 'btn', data: { confirm: "Are you sure?" } do
        .fa.fa-times
        Delete
