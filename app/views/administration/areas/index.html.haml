= render partial: "index_header"

.container-fluid
  .row
    .col
      .card
        .card-body
          - if @areas.size > 0

            = render partial: "index_list"
          - else
            %p
              No areas found

= javascript_include_tag "administration/areas/Index"

:javascript
  $(document).ready(function() {
    Index.init();
  });
