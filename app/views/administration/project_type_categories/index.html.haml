= render partial: "index_header"

.container-fluid
  .row
    .col
      .card
        .card-body
          - if @project_type_categories.any?
            = render partial: "index_list"
          - else
            %p
              No project type categories found.
