= render partial: "index_header"

.container-fluid
  .row
    .col
      .card
        .card-body
          - if @centers.size > 0

            = render partial: "index_list"
          - else
            %p
              No centers found

= javascript_include_tag "administration/centers/Index"

:javascript
  $(document).ready(function() {
    Index.init();
  });
