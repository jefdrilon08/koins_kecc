= render partial: "index_header"

.container-fluid
  .row
    .col
      .card
        .card-body
          - if @branches.any?

            = render partial: "index_list"
          - else
            %p
              No branches found

= javascript_include_tag "administration/branches/Index"

:javascript
  $(document).ready(function() {
    Index.init();
  });
