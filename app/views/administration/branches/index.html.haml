%main.c-main
  .container-fluid
    .row
      .col
        .card
          .card-body
            - if @branches.any?

              = render partial: "index_list"
            - else
              %p
                No branches found
