= render partial: "shared/c_subheader", locals: { items: @subheader_items, side_actions: @subheader_side_actions }

%main.c-main
  .container-fluid
    .row
      .col
        .card
          .card-body
            #survey-answer-content

#parameters{ data: { id: "#{@survey_answer.id}", member_id: "#{@member.id}", controller: "#{params[:controller]}", action: "#{params[:action]}" } }
