%table.table.table-sm.table-hover.table-bordered
  %thead
    %tr
      %th
        Type
      %th.text-right
        Maintaining Balance
      %th.text-right
        Current Balance
  %tbody
    - @savings_accounts.each do |o|
      %tr
        %td
          %strong
            = link_to savings_account_path(o.id) do
              #{o.account_subtype}
        %td.text-right.text-muted
          #{number_to_currency(o.maintaining_balance, unit: "")}
        %td.text-right
          %strong
            #{number_to_currency(o.balance, unit: "")}
