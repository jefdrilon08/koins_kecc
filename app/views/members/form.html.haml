= title
= render partial: "shared/c_subheader", locals: { items: @subheader_items, side_actions: @subheader_side_actions }

%main.c-main
  .container-fluid
    .row
      .col
        .card
          .card-body
            #content

#parameters{ data: { id: "#{params[:id]}", member_types: Settings.default_member_types, controller: "#{params[:controller]}", action: "#{params[:action]}" } }
