= title
%h4
  Insurance Monthly Closing Collections
.card
  .card-body
    = render partial: "index_filter"
    - if @insurance_monthly_closing_collections.any?
      = render partial: "index_list"
      = paginate @insurance_monthly_closing_collections
    - else
      %p
        No records found

= render partial: "insurance_monthly_closing_collections/modals/new"
= render partial: "templates/error_list"
