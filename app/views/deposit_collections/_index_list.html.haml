%table.table.table-sm.table-hover.table-bordered
  %thead
    %tr
      %th{ width: "5%" }
      %th
        OR Number
      %th
        Collection Date
      %th
        Date Approved
      %th
        Branch
      %th.text-right
        Amount Collected
      %th
        %center
          Status
      - if microinsurance?
        %th
          %center
            Finalize Stat
  %tbody
    - @deposit_collections.each do |o|
      %tr
        %td.text-center
          = link_to deposit_collection_path(o) do
            .fa.fa-search
        %td
          #{o.or_number}
        %td
          %strong
            #{o.collection_date.strftime("%b %d, %Y")}
        %td
          - if o.date_approved.present?
            %strong
              #{o.date_approved.strftime("%b %d, %Y")}
        %td
          #{o.branch}
        %td.text-right
          %strong
            #{number_to_currency(o.total_collected, unit: "")}
        %td
          %center
            - if o.pending?
              .badge.badge-danger
                Pending
            - elsif o.approved?
              .badge.badge-success
                Approved
            - elsif o.processing?
              .badge.badge-warning
                Processing
        - if microinsurance?
          %td
            %center
              - if o.pending? && o.finalized?
                .badge.badge-primary
                  For Apporval
              - elsif o.pending? && !o.finalized?
                .badge.badge-warning
                  For Finalize
              - elsif o.approved?
                .badge.badge-success
                  Approved