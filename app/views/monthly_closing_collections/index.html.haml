= title
= render partial: "shared/c_subheader", locals: { items: @subheader_items, side_actions: @subheader_side_actions }

%main.c-main
  .container-fluid
    .row
      .col
        .card
          .card-body
            = render partial: "index_filter"
            - if @monthly_closing_collections.any?
              = render partial: "index_list"
              = paginate @monthly_closing_collections
            - else
              %p
                No records found

= render partial: "monthly_closing_collections/modals/new"
= render partial: "templates/error_list"

#parameters{ data: { controller: "#{params[:controller]}", action: "#{params[:action]}" } }
