%center
  %h1
    There was an error when processing this monthly closing collection.
  %h2.text-muted
    #{@monthly_closing_collection.id}
%pre
  #{JSON.pretty_generate(JSON.parse(@monthly_closing_collection.data.to_json))}
